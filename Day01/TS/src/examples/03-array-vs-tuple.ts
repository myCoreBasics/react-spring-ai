// ============================================
// 예제 3: 배열 vs 튜플 비교 실습
// 실행: pnpm exec ts-node src/examples/03-array-vs-tuple.ts
// ============================================

console.log("╔════════════════════════════════════════╗");
console.log("║     배열(Array) vs 튜플(Tuple) 비교     ║");
console.log("╚════════════════════════════════════════╝\n");

// ============================================
// 1️⃣ 배열 (Array) - 같은 타입, 가변 길이
// ============================================
console.log("=== 1. 배열 (Array) ===");
console.log("특징: 같은 타입의 요소들, 길이 변경 가능\n");

// 문자열 배열
const fruits: string[] = ["사과", "바나나", "오렌지"];
console.log("과일 배열:", fruits);

// 배열에 요소 추가 (가변 길이)
fruits.push("포도");
fruits.push("딸기");
console.log("추가 후:", fruits);

// 숫자 배열
const numbers: number[] = [10, 20, 30, 40, 50];
console.log("숫자 배열:", numbers);

// 제네릭 문법으로 선언
const scores: Array<number> = [95, 88, 92, 78, 85];
console.log("점수 배열:", scores);

// ============================================
// 2️⃣ 튜플 (Tuple) - 위치별 타입, 고정 길이
// ============================================
console.log("\n=== 2. 튜플 (Tuple) ===");
console.log("특징: 위치별로 다른 타입, 길이 고정\n");

// 기본 튜플: [문자열, 숫자]
const userInfo: [string, number] = ["홍길동", 25];
console.log("사용자 정보:", userInfo);
console.log(`  - 이름(0번): ${userInfo[0]}`);
console.log(`  - 나이(1번): ${userInfo[1]}`);

// 좌표 튜플: [숫자, 숫자]
const coordinate: [number, number] = [37.5665, 126.978];
console.log("좌표:", coordinate);

// 3개 요소 튜플: [숫자, 숫자, 숫자]
const rgb: [number, number, number] = [255, 128, 0];
console.log("RGB 색상:", rgb);

// 다양한 타입 튜플: [불리언, 문자열, 숫자]
const result: [boolean, string, number] = [true, "성공", 200];
console.log("결과:", result);

// ============================================
// 3️⃣ 핵심 차이점 비교
// ============================================
console.log("\n=== 3. 핵심 차이점 ===\n");

console.log("┌──────────────┬─────────────────┬─────────────────┐");
console.log("│     특징     │   배열 (Array)   │  튜플 (Tuple)   │");
console.log("├──────────────┼─────────────────┼─────────────────┤");
console.log("│   요소 타입   │   모두 동일      │  위치별 다름    │");
console.log("│     길이     │   가변적         │  고정적         │");
console.log("│     용도     │   목록/컬렉션    │  복합 데이터    │");
console.log("└──────────────┴─────────────────┴─────────────────┘");

// ============================================
// 4️⃣ 실제 사용 예시
// ============================================
console.log("\n=== 4. 실제 사용 예시 ===\n");

// 예시 1: 함수에서 여러 값 반환 (튜플)
function getMinMax(nums: number[]): [number, number] {
  const min = Math.min(...nums);
  const max = Math.max(...nums);
  return [min, max];  // 튜플로 반환
}

const testNumbers = [3, 1, 4, 1, 5, 9, 2, 6];
const [min, max] = getMinMax(testNumbers);  // 구조 분해 할당
console.log("예시 1: 함수에서 여러 값 반환");
console.log(`  입력: [${testNumbers.join(", ")}]`);
console.log(`  최소값: ${min}, 최대값: ${max}`);

// 예시 2: 키-값 쌍 목록 (튜플 배열)
const productPrices: [string, number][] = [
  ["사과", 1000],
  ["바나나", 500],
  ["오렌지", 800],
];

console.log("\n예시 2: 키-값 쌍 (튜플 배열)");
productPrices.forEach(([name, price]) => {
  console.log(`  ${name}: ${price.toLocaleString()}원`);
});

// 예시 3: API 응답 형태 (튜플)
type ApiResponse = [boolean, string, object | null];

const successResponse: ApiResponse = [true, "성공", { id: 1, name: "데이터" }];
const errorResponse: ApiResponse = [false, "에러 발생", null];

console.log("\n예시 3: API 응답 형태");
console.log("  성공:", successResponse);
console.log("  실패:", errorResponse);

// 예시 4: 선택적 요소가 있는 튜플
const optionalTuple: [string, number, string?] = ["홍길동", 25];
console.log("\n예시 4: 선택적 요소 튜플");
console.log("  2개만:", optionalTuple);

const fullTuple: [string, number, string?] = ["김철수", 30, "서울"];
console.log("  3개:", fullTuple);

// ============================================
// 5️⃣ 타입 안전성 비교
// ============================================
console.log("\n=== 5. 타입 안전성 ===\n");

// 배열: 인덱스에 관계없이 같은 타입
const arr: string[] = ["a", "b", "c"];
// arr[0] = 123;  // ❌ 에러! string만 가능

// 튜플: 위치별로 타입 체크
const tuple: [string, number] = ["이름", 100];
// tuple[0] = 123;      // ❌ 에러! 0번은 string
// tuple[1] = "문자열"; // ❌ 에러! 1번은 number
// tuple[2] = "추가";   // ❌ 에러! 2번 인덱스 없음

console.log("배열: 모든 인덱스에 string 타입 적용");
console.log("튜플: 0번=string, 1번=number 엄격히 체크");

// ============================================
// 6️⃣ 런타임에서는 동일
// ============================================
console.log("\n=== 6. 런타임 동작 ===\n");

console.log("⚠️ 중요: JavaScript에는 튜플이 없습니다!");
console.log("   TypeScript의 튜플은 컴파일 시점에만 타입 체크됩니다.");
console.log("   런타임에서는 둘 다 일반 배열(Array)로 동작합니다.\n");

console.log(`Array.isArray(fruits): ${Array.isArray(fruits)}`);
console.log(`Array.isArray(userInfo): ${Array.isArray(userInfo)}`);

console.log("\n✅ 예제 3 완료!");
console.log("💡 Tip: 튜플은 '의미 있는 순서'가 있는 복합 데이터에 사용하세요!");

