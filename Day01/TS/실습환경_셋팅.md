# 🛠️ TypeScript 실습환경 셋팅 가이드

---

## 📋 사전 준비

### 필수 설치 프로그램

| 프로그램 | 버전 | 설치 확인 |
|----------|------|-----------|
| Node.js | 18 이상 | `node --version` |
| pnpm (또는 npm) | 최신 버전 | `pnpm --version` |
| VS Code 또는 Cursor | 최신 버전 | - |

### Node.js 설치 (미설치 시)

```bash
# Homebrew로 설치 (macOS)
brew install node

# 또는 공식 사이트에서 다운로드
# https://nodejs.org/
```

### pnpm 설치 (미설치 시)

```bash
# npm으로 설치
npm install -g pnpm

# 또는 Homebrew로 설치 (macOS)
brew install pnpm
```

---

## 🚀 프로젝트 셋팅

### 1단계: 프로젝트 폴더 생성

```bash
mkdir my-typescript-practice
cd my-typescript-practice
```

### 2단계: 패키지 초기화

```bash
pnpm init
```

### 3단계: TypeScript 및 필수 패키지 설치

```bash
pnpm add -D typescript ts-node @types/node
```

| 패키지 | 역할 |
|--------|------|
| `typescript` | TypeScript 컴파일러 |
| `ts-node` | TypeScript 파일 직접 실행 |
| `@types/node` | Node.js 타입 정의 |

### 4단계: tsconfig.json 생성

```bash
pnpm tsc --init
```

또는 아래 내용으로 직접 생성:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "CommonJS",
    "lib": ["ES2022"],
    "types": ["node"],
    "rootDir": "./src",
    "outDir": "./dist",
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "sourceMap": true
  },
  "include": ["src/**/*", "*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

### 5단계: 폴더 구조 생성

```bash
mkdir -p src/examples src/exercises
```

### 6단계: package.json 스크립트 추가

```json
{
  "name": "typescript-practice",
  "version": "1.0.0",
  "description": "TypeScript 기본 문법 실습",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "example:1": "ts-node src/examples/01-basic-types.ts",
    "example:2": "ts-node src/examples/02-functions.ts",
    "exercise:1": "ts-node src/exercises/exercise-01.ts"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
```

---

## 📁 최종 프로젝트 구조

```
typescript-practice/
├── src/
│   ├── examples/              # 예제 코드
│   │   ├── 01-basic-types.ts
│   │   └── 02-functions.ts
│   └── exercises/             # 연습 문제
│       └── exercise-01.ts
├── dist/                      # 컴파일된 JS 파일 (빌드 후 생성)
├── node_modules/              # 의존성 패키지
├── index.ts                   # 메인 실습 파일
├── tsconfig.json              # TypeScript 설정
├── package.json               # 프로젝트 설정
└── pnpm-lock.yaml             # 의존성 잠금 파일
```

---

## ⚡ 실행 방법

### 스크립트로 실행

```bash
# 메인 파일 실행
pnpm start

# 예제 실행
pnpm example:1
pnpm example:2

# 연습 문제 실행
pnpm exercise:1

# TypeScript → JavaScript 컴파일
pnpm build
```

### 원하는 파일 직접 실행

```bash
# 방법 1: pnpm exec 사용
pnpm exec ts-node 파일명.ts

# 방법 2: npx 사용
npx ts-node 파일명.ts

# 예시
pnpm exec ts-node src/examples/01-basic-types.ts
```

---

## ⚠️ 자주 발생하는 문제

### 1. `tsc: command not found`

**원인**: TypeScript가 전역 설치되지 않음

**해결**: `pnpm tsc` 또는 `npx tsc` 사용

```bash
# ❌ 안됨
tsc --version

# ✅ 됨
pnpm tsc --version
npx tsc --version
```

### 2. `Cannot find module 'ts-node'`

**원인**: ts-node가 설치되지 않음

**해결**:
```bash
pnpm add -D ts-node
```

### 3. `Cannot find module '@types/node'`

**원인**: Node.js 타입 정의가 없음

**해결**:
```bash
pnpm add -D @types/node
```

### 4. 타입 에러가 너무 많이 발생

**해결**: tsconfig.json에서 strict 옵션을 임시로 완화

```json
{
  "compilerOptions": {
    "strict": false,           // 일단 끄고 학습
    "noImplicitAny": false,    // any 허용
    "strictNullChecks": false  // null 체크 완화
  }
}
```

> 💡 학습 초반에는 느슨하게 설정하고, 익숙해지면 strict 모드를 켜세요!

---

## 🔧 VS Code / Cursor 추천 확장 프로그램

| 확장 프로그램 | 설명 |
|--------------|------|
| **TypeScript Hero** | 자동 import 정리 |
| **Error Lens** | 에러 인라인 표시 |
| **Pretty TypeScript Errors** | 에러 메시지 가독성 향상 |
| **Code Runner** | 코드 바로 실행 |

---

## 🎯 실습 체크리스트

- [ ] Node.js 설치 확인
- [ ] pnpm 설치 확인
- [ ] TypeScript 패키지 설치
- [ ] tsconfig.json 생성
- [ ] 폴더 구조 생성
- [ ] 예제 파일 실행 테스트
- [ ] 첫 번째 TypeScript 코드 작성

---

## 📝 빠른 시작 (한 번에 셋팅)

이미 Node.js와 pnpm이 설치되어 있다면:

```bash
# 1. 프로젝트 폴더로 이동
cd /path/to/your/project

# 2. 패키지 초기화 + 설치
pnpm init -y
pnpm add -D typescript ts-node @types/node

# 3. tsconfig.json 생성
pnpm tsc --init

# 4. 폴더 구조 생성
mkdir -p src/examples src/exercises

# 5. 첫 번째 파일 생성
echo 'console.log("Hello, TypeScript!");' > index.ts

# 6. 실행 테스트
pnpm exec ts-node index.ts
```

**출력 결과**:
```
Hello, TypeScript!
```

축하합니다! 🎉 TypeScript 실습 환경이 준비되었습니다!

